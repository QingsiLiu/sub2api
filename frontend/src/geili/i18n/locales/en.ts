/**
 * Geili English messages. Everything lives under the `geili` namespace so it can
 * never collide with upstream keys.
 */
export default {
  geili: {
    brand: {
      name: 'Geili API',
      tagline: 'A stable, transparent and traceable AI model gateway'
    },
    system: {
      selfUpdateDisabled:
        'This site is upgraded through the Geili release pipeline; in-place self-update is disabled. New upstream versions are announced here and shipped after the operators merge them.'
    }
  }
}
